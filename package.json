{
  "name": "navigator",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "devDependencies": {
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.11.3",
    "typescript": "^5.3.3"
  },
  "scripts": {
    "test:regression": "playwright test --config=playwright.config.regression.ts --headed --reporter=html",
    "test:smoke": "playwright test --config=playwright.config.regression.ts --headed --reporter=html --grep @smoke"
  }
}
